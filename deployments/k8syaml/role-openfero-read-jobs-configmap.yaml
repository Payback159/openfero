# Source: openfero/templates/role-read-jobs.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  annotations:
    description: "Allow reading job-definitions configMap"
    rbac.authorization.kubernetes.io/autoupdate: "true"
  name: openfero-read-jobs-configmap
  namespace: openfero
  labels:
    helm.sh/chart: openfero-0.1.0
    app.kubernetes.io/name: openfero
    app.kubernetes.io/instance: openfero
    app.kubernetes.io/version: "0.7.1"
    app.kubernetes.io/managed-by: Helm
rules:
  - resources:
    - configmaps
    apiGroups: [""]
    verbs:
    - get
    - list
